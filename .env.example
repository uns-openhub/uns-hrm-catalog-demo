# Set these for a process started directly on the controller's development host.
# Controller-managed RTT instances receive them from the controller.
UNS_CONTROLLER_NAME=controller-local
UNS_CONTROLLER_PUBLIC_BASE=http://localhost:3200
